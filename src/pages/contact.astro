---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Contact Us | Bolean">
  <Navbar />
  <main style="min-height: calc(100vh - 80px); padding-top: 80px;">
    <section class="section section-light" style="min-height: calc(100vh - 80px);">
      <div class="container">
        <div class="section-header">
          <span class="section-subtitle">Contact Us</span>
          <h2 class="section-title">Let's Work Together</h2>
        </div>
        
        <div class="contact-grid">
          <div class="contact-info">
            <h3>Get In Touch</h3>
            <div class="info-item">
              <div class="info-icon">📍</div>
              <div class="info-text">
                <h4>Our Location</h4>
                <p>Jakarta, Indonesia</p>
              </div>
            </div>
            <div class="info-item">
              <div class="info-icon">📧</div>
              <div class="info-text">
                <h4>Email Us</h4>
                <p>info@bolean.com</p>
              </div>
            </div>
            <div class="info-item">
              <div class="info-icon">📞</div>
              <div class="info-text">
                <h4>Call Us</h4>
                <p>+62 123 4567 890</p>
              </div>
            </div>
          </div>
          
          <div class="contact-form-wrapper">
            <div id="form-success" class="text-center py-8" style="display: none;">
              <h3 style="color: var(--color-primary); margin-bottom: 1rem;">Message Sent!</h3>
              <p>We will get back to you shortly.</p>
              <button id="btn-reset" class="btn btn-secondary" style="margin-top: 2rem;">Send Another</button>
            </div>
            
            <form id="contact-form">
              <div class="form-group">
                <input 
                  type="text" 
                  id="form-name"
                  class="form-control" 
                  placeholder="Your Name" 
                  required 
                />
              </div>
              <div class="form-group">
                <input 
                  type="email" 
                  id="form-email"
                  class="form-control" 
                  placeholder="Email Address" 
                  required 
                />
              </div>
              <div class="form-group">
                <textarea 
                  id="form-message"
                  class="form-control" 
                  placeholder="How can we help you?" 
                  required
                ></textarea>
              </div>
              <button type="submit" id="btn-submit" class="btn btn-primary">
                Send Message
              </button>
            </form>
          </div>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</Layout>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('contact-form');
    const successMsg = document.getElementById('form-success');
    const btnSubmit = document.getElementById('btn-submit');
    const btnReset = document.getElementById('btn-reset');

    if (form && successMsg && btnSubmit && btnReset) {
      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        
        // Disable button
        btnSubmit.textContent = 'Sending...';
        btnSubmit.setAttribute('disabled', 'true');
        
        // Simulate API call to Supabase
        await new Promise(resolve => setTimeout(resolve, 1000));
        
        // Show success
        form.style.display = 'none';
        successMsg.style.display = 'block';
        
        // Reset form
        (form as HTMLFormElement).reset();
        
        // Reset button
        btnSubmit.textContent = 'Send Message';
        btnSubmit.removeAttribute('disabled');
      });

      btnReset.addEventListener('click', () => {
        successMsg.style.display = 'none';
        form.style.display = 'block';
      });
    }
  });
</script>
